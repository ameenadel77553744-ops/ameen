@using System.Globalization
@{
    var isArabic = CultureInfo.CurrentUICulture.TwoLetterISOLanguageName == "ar";
    ViewData["Title"] = isArabic ? "تسجيل الدخول" : "Login";
}

<div class="container py-5" style="max-width:480px;">
    <div class="text-center mb-4">
        <h1 class="display-6 fw-bold futuristic-text saudi-glow">🇸🇦 السوق السعودي الإلكتروني 2030</h1>
        <p class="text-muted">@(isArabic ? "يرجى تسجيل الدخول للوصول إلى المتجر" : "Please login to access the store")</p>
    </div>
    <div class="card saudi-card holographic shadow-lg">
        <div class="card-body p-4">
            <h2 class="mb-4 text-center">@ViewData["Title"]</h2>
            <form method="post" asp-controller="Auth" asp-action="Login">
                <input type="hidden" name="returnUrl" value="@ViewBag.ReturnUrl" />
                <div class="mb-3">
                    <label class="form-label">@(isArabic ? "اسم المستخدم" : "Username")</label>
                    <input class="form-control" type="text" name="username" required />
                </div>
                <div class="mb-3">
                    <label class="form-label">@(isArabic ? "كلمة المرور" : "Password")</label>
                    <input class="form-control" type="password" name="password" required />
                </div>
                @if (!ViewData.ModelState.IsValid)
                {
                    <div class="alert alert-danger">@(isArabic ? "بيانات الدخول غير صحيحة" : "Invalid credentials")</div>
                }
                <button class="btn btn-primary w-100" type="submit">@(isArabic ? "تسجيل الدخول" : "Sign in")</button>
                <div class="mt-3 text-center">
                    <small class="text-muted">@(isArabic ? "المدير: admin / admin" : "Admin: admin / admin")</small>
                </div>
            </form>
        </div>
    </div>
</div>


